import pytest

def test_prompt(spark):
    """
    prompt used:

    populate test_prompt() function body by using available functions from parrotpy.functions, generate new dataframe with a new column called "name" with values are people's name, another column called "age" with values from uniform distribution with min value 0 and max value 80.
    """
    pass

